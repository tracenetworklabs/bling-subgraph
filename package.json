{
  "name": "tracebling",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ keerthanaramalingam/tracebling",
    "create-local": "graph create --node http://localhost:8020/ keerthanaramalingam/tracebling",
    "remove-local": "graph remove --node http://localhost:8020/ keerthanaramalingam/tracebling",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 keerthanaramalingam/tracebling"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0",
    "graphql-type-json": "^0.3.2"
  }
}
